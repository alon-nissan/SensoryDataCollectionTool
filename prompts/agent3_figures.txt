You are extracting numerical data from a figure in a sensory science paper. A previous extraction step has already extracted tabulated data into flat observation rows. Your job is to extract ADDITIONAL data visible in this figure that is NOT already captured.

Paper ID: {paper_id}

== FIGURE INFORMATION ==

Caption: {figure_caption}

Description from article inventory: {figure_description}

== EXISTING DATA (skip duplicates) ==

Already-extracted observations (skip any values that duplicate these):
{existing_observations_summary}

Experiment context (method, scale, range):
{experiment_context}

== INSTRUCTIONS ==

1. Examine the figure carefully and identify every numerical data point visible.
2. Skip values already present in existing observations. If a value for the same (substance, concentration, attribute) combination already exists, do NOT re-extract it. Record skipped values in "skipped_values".
3. For each NEW data point, create a flat observation row with all fields below.
4. For derived metrics visible in figures (e.g., fitted curve parameters, R² values shown in legends), use value_type "derived_param" with components: null, base_matrix: null.

== READING FIGURES ==

- Bar charts: Read the bar height against the y-axis. Read error bars (top of bar to top of error bar = error value). Note whether error bars are SE or SD (check the caption/legend).
- Line plots: Read each data point where a marker appears. Interpolate from gridlines.
- Spider/radar plots: Read each spoke value for each sample.
- Dose-response curves: Read each plotted point along the curve. Also extract fitted parameters (slopes, EC50) if shown in legend/annotation.
- Box plots: Extract median, Q1, Q3, whisker endpoints if readable.
- PCA/MFA biplots: Do NOT extract coordinates as sensory data — note in extraction_notes if relevant loadings are visible.
- Heatmaps: Read cell values if labeled; otherwise estimate from the color scale.

== CONFIDENCE RATING ==

Rate extraction_confidence for each value:
- "high": Value readable within ±2% of scale range, clear gridlines or labeled data points.
- "medium": Value estimated within ±5–10% of scale range, interpolated between gridlines.
- "low": Value estimated with >10% uncertainty, overlapping elements, poor resolution, or ambiguous reading.

== OUTPUT FORMAT ==

Return a JSON object:

{
  "new_observations": [
    {
      "experiment": "exp1",
      "substance": "sucrose",
      "components": [{"substance": "sucrose", "concentration": 0.3, "unit": "M"}],
      "base_matrix": "distilled water",
      "attribute": "sweetness intensity",
      "attribute_normalized": "sweetness",
      "value": 42.5,
      "value_type": "raw_mean",
      "error": 3.2,
      "error_type": "se",
      "source": "Figure 1A",
      "source_type": "figure",
      "confidence": "medium"
    }
  ],
  "skipped_values": ["List of values skipped because they match existing observations"],
  "extraction_notes": "Free text about extraction quality, issues, legibility, or anything notable"
}

Field notes:
- experiment: Use "exp1", "exp2", etc. matching the experiment context provided.
- value_type: One of "raw_mean", "raw_median", "frequency_pct", "dominance_rate", "relative_pct", "derived_param", "threshold".
- error_type: One of "se", "sd", "ci95_lower", "ci95_upper", null.
- confidence: One of "high", "medium", "low".
- For stimulus-level derived metrics (slopes, EC50, etc.): set components and base_matrix to null; set value_type to "derived_param".

Return ONLY the JSON.
